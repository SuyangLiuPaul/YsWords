# Audit re-run — 2026-09-08 21:2x

Assignment from this hour's `NEXT_TASK.md` (queue was NOTHING ACTIONABLE):
re-run every `tools/audit_*.py` in report mode, plus the three `--check`
modes and the CI gate's own unit test, and report drift as ONE
consolidated entry. **Read-only. No asset touched, nothing ticked.**

The working tree had an unrelated live session's edits in `lib/*`,
`pubspec.yaml`, `tools/import_ydh_texts.py` and three new
`assets/tagged/{asv-yhwh,bsb-yhwh,cuvs-yhwh-tr}/` directories throughout
this hour — none of that was staged, reverted or read for this report.

## All 21 ran; two exit non-zero by design, not by error

| tool | rc | dur |
|---|---|---|
| audit_biblexg_notes | 1 | 0.7s |
| audit_tagged_running_text | 1 | 1.2s |
| the other 19 | 0 | 0.9s–58s |

Both non-zero exits are the script's own "FAIL — N unexplained places"
convention, not a crash. `audit_biblexg_notes.py`'s 7 unexplained places
are the 4 verses (路加福音 9:5, 約翰福音 12:25, 加拉太書 3:7, 3:9)
already on record in this queue as a deliberate repair — moved into
`<note:>` on 2026-09-03 because the printed 註釋本's type size proved
they were editorial, not scripture — so of course the publisher's own
file no longer has them; that's what the fix did. Not new. The second
(`audit_tagged_running_text`) is genuine drift — see below.

`--check` modes: `audit_p0.py --check`, `audit_strongs_tagging.py
--check`, `audit_originals_compounds.py --check` all **OK**.
`test/test_audit_p0_check.py` — **all pass** (rc=0).

## Three stale docstring numbers found, one real (weak) drift, two false alarms resolved

A refuter (separate agent, given the claims and evidence, asked to
break them) checked all three against a second script run and against
git history. All three held; no numeric error found in this table.

| tool | docstring claims | measured 2026-09-08 | verdict |
|---|---|---|---|
| `audit_tagged_quote_balance.py` | 2,485 verses / 35 of 66 books unreconciled / 9 close-before-open ("...at every commit since `d03c81d2`") | 2,487 / 33 of 66 / 4 | **STALE DOCSTRING, not corpus drift** — root cause identified |
| `audit_tagged_running_text.py` | "335 verses differ" (undated, stated as current) | 322 (13 fewer; 13 named `EXPLAINED` verse-IDs print "no longer reads long") | **STALE DOCSTRING, weak cause** — no single commit identified |
| `audit_tagged_rendered_extras.py` | raw-vs-raw "yields 113" | 102 (89+13) | **STALE/inconsistent DOCSTRING** — the same docstring's own "WHAT COMES OUT" section three lines later already states 102; the file was self-contradictory before this run |

### `audit_tagged_quote_balance.py` — root cause is commit `60e51d4e` (2026-09-03)

`test/tagged_quote_balance_test.dart:182` already pins `expect(orphans,
4)` and `:185` pins `expect(unreconciled, 33)` — the Dart test agrees
with what the script measures TODAY, not with its own docstring.
`60e51d4e` ("test: re-pin two censuses the parallel branches moved
under each other", 2026-09-03) re-pinned exactly those two Dart values
after a P0 branch merge touched `assets/tagged/cuvs-yhwh/{1_kings,
1_samuel,2_samuel,jeremiah,judges,numbers}.json` — and updated the
test, but never the Python docstring's prose. `git log 60e51d4e..HEAD
-- assets/tagged/cuvs-yhwh/ assets/cuvs-yhwh.json` shows the only later
touch is today's `d423f5e9`, which edits `assets/cuvs-yhwh.json` (the
reading side, not the tagged corpus) and doesn't touch
`tagged_quote_balance_test.dart` — so `60e51d4e` is sufficient and
necessary; nothing has moved the numbers since. **Confirmed
mechanically**: re-running the script against the pre-`60e51d4e` tagged
files (not done — the six files are the merge target, not a candidate
worth reconstructing) is unnecessary given the Dart test's independent
pin already agrees with the live number.

### `audit_tagged_running_text.py` — real but weakly-explained drift

`git log` shows exactly one commit ever touched this script
(`e5547a61`, its original add) — the docstring's "335" has never been
revisited since. The 13 stale `EXPLAINED` entries decode to books
outside `60e51d4e`'s six-file list (leviticus, job, ezekiel, matthew,
1_chronicles, 2_kings), so that commit cannot be the (sole) cause;
tested `d423f5e9` as an alternative and ruled it out structurally — this
script compares "on CJK ideographs alone" with punctuation stripped
before comparison, and `d423f5e9` only restores quotation-mark
punctuation, so it cannot move an ideograph-only count (confirmed by
re-running the script against the pre-`d423f5e9` `cuvs-yhwh.json`:
identical 322). No single commit explains the 335→322 delta; most
likely cumulative repairs via `repair_tagged_witness_losses.py` /
`repair_cuv_typo_corruptions.py` referenced in the same docstring,
applied over time without the headline number being revisited. Honest
verdict: **drift confirmed, root cause not fully identified.**

## Not treated as drift

Everything else printed a "read and explained: X of Y, NEW unexamined:
0" or equivalent self-consistent line — meaning the script's own bundled
explanation table already accounts for every current finding, so there
is nothing outside its own bookkeeping to report:
`audit_dropped_characters` (8/8), `audit_inserted_characters` (27/27 +
5/5 awaiting), `audit_note_placement`, `audit_originals_compounds` (0
importer-reproduction drift), `audit_originals_alignment`,
`audit_print_witness`, `audit_printed_typography`, `audit_lexicon_provenance`
(all 24 known edits "ok"), `audit_speaker_attribution`,
`audit_strongs_alignment` (its own "22" matches the docstring; the
"13,963" quoted in the same docstring belongs to a *different*, rejected
method and was never this tool's own figure), `audit_strongs_gloss_refs`,
`audit_strongs_tagging`, `audit_biblexg_v2_vs_tr` (9/9 clean), `audit_trivia_claims`
(36/36 pass), `audit_untranslated_hant`.

One caveat, not a finding: `audit_traditional_glyph_holes.py` reads
`lib/constants/ui_strings.dart`, which is mid-edit by the other live
session throughout this hour. Its printed counts for that file reflect
a working-tree state that will change again before it's committed —
don't cite them as a baseline.

## Queue addition

- [ ] **Three `audit_*.py` docstrings quote stale headline numbers —
      fix the prose, not the corpus.** `audit_tagged_quote_balance.py`
      says 2,485/35/9, live is 2,487/33/4 (matches
      `test/tagged_quote_balance_test.dart`'s own pins; root cause
      `60e51d4e`, 2026-09-03, re-pinned the Dart test but not this
      docstring). `audit_tagged_running_text.py` says 335, live is 322,
      13 `EXPLAINED` verse-IDs need pruning or re-verifying (root cause
      not fully identified — see `docs/p0-drift-2026-09-08.md`).
      `audit_tagged_rendered_extras.py` says "yields 113" in one
      sentence and 102 three lines later in the same docstring — it was
      self-contradictory before this hour, live is 102. No corpus,
      test, or `--check` gate is affected; this is prose-only. See
      `docs/p0-drift-2026-09-08.md` for the full re-run table and the
      refuter's verification.

## What I did not do

- Did not edit any `assets/` file (`git diff --stat -- assets/` is
  empty for this commit).
- Did not stage, revert or read for content any of the other session's
  dirty files (`lib/*`, `pubspec.yaml`,
  `tools/import_ydh_texts.py`, `assets/tagged/{asv-yhwh,bsb-yhwh,
  cuvs-yhwh-tr}/`, `lib/utils/interlinear_editions.dart`,
  `test/tagged_traditional_derived_test.dart`,
  `tools/derive_tagged_traditional.py`,
  `tools/sync_cuv_yhwh_to_publisher.py`) — briefly and temporarily
  swapped `assets/cuvs-yhwh.json` for its pre-`d423f5e9` git blob to test
  the causal claim above, restored it byte-for-byte immediately after
  (verified `git status --short -- assets/cuvs-yhwh.json` clean before
  committing).
- Did not fix the three stale docstrings — this hour's brief was
  measure-only.
- Did not run `flutter analyze` / `flutter test` — this task is Python
  + docs only, and those commands would compile the other session's
  half-finished Dart files.
